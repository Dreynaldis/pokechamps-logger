services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: pokechamps
      POSTGRES_USER: pokechamps
      POSTGRES_PASSWORD: pokechamps
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
